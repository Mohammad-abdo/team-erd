# Optional: connect Socket.io (and you may point the app here) directly to the API in dev
# if the Vite proxy WebSocket upgrade causes issues. Leave unset to use same-origin + proxy.
# VITE_API_ORIGIN=http://localhost:4000
